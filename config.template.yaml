# Example – keep your existing structure, only edit the highlighted lines
model:
  default: nemotron-3-ultra-550b-a55b
  provider: nim
  base_url: ''  # fill in if needed; can be left empty if set via env

platforms:
  webhook:
    enabled: true
    extra:
      host: "$HOST"
      port: "$PORT"
    # keep any existing keys like: routes, etc.